{
  "name": "dsgn-monorepo",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace site",
    "build:registry": "npm run build:registry --workspace @dsgn/registry",
    "sync-registry": "node scripts/sync-registry.mjs",
    "typecheck": "npm run typecheck --workspace @dsgn/registry",
    "lint:registry-tokens": "node scripts/lint-registry-tokens.mjs",
    "lint:registry-imports": "node scripts/lint-registry-imports.mjs",
    "lint:skill-drift": "node scripts/lint-skill-drift.mjs"
  },
  "dependencies": {
    "@tailwindcss/oxide": "^4.3.3",
    "lightningcss": "^1.33.0"
  }
}
