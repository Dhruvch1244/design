node_modules/
.DS_Store
*.log

# Build artifacts generated from packages/registry/src — regenerated by
# `npm run sync-registry` (also runs automatically via apps/site's
# predev/prebuild), never edited directly.
packages/registry/dist/
apps/site/public/r/

# Synced from skills/dsgn (the real source) by scripts/sync-skill.mjs, also
# run automatically via prepublishOnly — never edited directly.
packages/cli/skill/
